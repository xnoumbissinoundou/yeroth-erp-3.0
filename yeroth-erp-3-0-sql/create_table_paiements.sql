create table paiements (id int auto_increment primary key, date_paiement date NOT NULL, heure_paiement time NOT NULL, nom_entreprise varchar(256) NOT NULL, nom_encaisseur varchar(256) NOT NULL, montant_paye double NOT NULL, compte_client double NOT NULL, type_de_paiement varchar(256) NOT NULL, engagement varchar(256) NOT NULL);
